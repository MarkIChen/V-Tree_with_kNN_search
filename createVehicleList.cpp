#include "controller/V-Tree.h"
#include <iostream>
#include <vector>

using namespace std;

int Vehicle::vehicleCount = 0;

int main() {

  // vector<Vehicle> vehicleList;
  // vehicleList.push_back(Vehicle(4, 2, 4));
  // vehicleList.push_back(Vehicle(5, 8, 5));
  // vehicleList.push_back(Vehicle(8, 6, 8));
  //
  // for (int i = 0; i < vehicleList.size(); i++) {
  //   cout << "car index = " << vehicleList[i].getintVehicleIndex();
  //   cout << ", edge 1 = " << vehicleList[i].getEdgeVertexIndexFirst();
  //   cout << ", edge 2 = " << vehicleList[i].getEdgeVertexIndexSecond();
  //   cout << ", des = " << vehicleList[i].getDesVertexIndex() <<endl;
  // }

  ActiveObject ao1(1, 10);

  return 0;
}
